# Auth & secrets
auths/
data/
*.key
*.pem
*.env
.env
.env.*

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
/login
/credit
/wb2api
/signin_bin

# Config with secrets (use config.example.json)
config.json

# Build artifacts
*.tmp
__pycache__/

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md

# 设计/计划文档（但 README.md 必须进版本库）
*.md
!README.md

# 文档库白名单（本 fork 特有，上游禁用 docs/）
#
# 上游把 docs/ 与全部 *.md 排除，是「工作文档不进版本库」的纪律。本 fork 例外：
# 项目文档库需要随仓库版本化，理由与边界见 docs/AGENTS.md。
# 放宽的只是版本控制；auths/ data/ config.json 等敏感文件仍受上方规则约束，不受影响。
!.agents/
!.agents/**
!docs/
!docs/**
!AGENTS.md
!CLAUDE.md
